machine:
  python:
    version: 3.5.2
  environment:
    PIP_EXISTS_ACTION: w
test:
  override:
    - "make SPHINXOPTS=-W html"
deployment:
  production:
    branch: master
    commands:
      - ./upload.sh
